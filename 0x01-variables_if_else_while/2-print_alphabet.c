#include <stdio.h>

/**
 * main - main function
 * Description - prints alphabet with putchar
 * Return: 0
 */
int main(void)
{
	char az;

	for (az = 'a'; az <= 'z'; az++)
	{
		putchar(az);
	}
	return (0);
}
